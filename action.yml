name: dhall-resolve
description: Build and package Dhall sources as one file
author: Nikita Volkov
branding:
  color: blue
inputs:
  file:
    description: Path to source file
    required: true
  cse:
    description: Apply common subexpression elimination to reduce output size
    required: false
    default: 'false'
outputs:
  path:
    description: Path to the generated file
runs:
  using: docker
  image: docker://ghcr.io/nikita-volkov/dhall-resolve.github-action:192250e70e56f859d0132c34f773800d0b9b54d1
  env:
    file: ${{ inputs.file }}
    cse: ${{ inputs.cse }}
